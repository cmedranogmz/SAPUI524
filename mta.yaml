_schema-version: "3.2"
ID: DemoSAPUI5
version: 0.0.1
modules:
- name: DemoSAPUI5-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: DemoSAPUI5-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - cmgfiori.zip
      name: cmgfiori
      target-path: resources/
- name: cmgfiori
  type: html5
  path: fiori
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: DemoSAPUI5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: DemoSAPUI5-html5-srv
    service-plan: app-host
- name: DemoSAPUI5-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: DemoSAPUI5-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
